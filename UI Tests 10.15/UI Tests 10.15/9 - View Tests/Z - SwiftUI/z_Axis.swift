import SwiftUI
import SwiftUIJson

struct z_Axis: View {
    var body: some View {
        VStack {
            Text("Axis")
            VStack {
            }
        }
    }
}

struct z_Axis_Previews: PreviewProvider {
    static var previews: some View {
        JsonPreview {
            z_Axis()
        }
    }
}
